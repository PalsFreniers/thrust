addin "typ"

fn cstrlen ptr -> int as
	alloc s sizeof ptr end s .ptr
	if s ,64 0 = go
		0 ret
	end
	0 during dup s ,ptr + ,8 0 != go
		1 +
	end
end

define _cstrToStr
	dup cstrlen <>
end

fn cstrToStr ptr -> int ptr as
	_cstrToStr
end
